{
  "name": "penny-pixel-pop-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic envelope engine + routing math for Sequence-derived data.",
  "type": "commonjs",
  "main": "dist/sequenceClient.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/tests/*.test.js dist/*/__tests__/*.test.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.90.1",
    "@vercel/node": "^5.5.22",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "typescript": "^5.6.3"
  }
}
